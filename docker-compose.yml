services:
  postgres:
    image: postgres:16
    container_name: maidshowcase-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: maidshowcase
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
